<!DOCTYPE html>
<html>
<head>
	<title>Welcome!</title>
	<link rel="stylesheet" type="text/css" href="style.css">
	<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
	<link rel="stylesheet" href="http://fonts.googleapis.com/earlyaccess/nanumgothic.css">
	<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css" rel="stylesheet"/>
</head>
<body>
	<div class="maintitle" align="center">
		<p style="color:white"> 금식수련회에 오신 것을 환영합니다!</p>
		<p style="color:white"> 아래 목록에서 원하는 <주제강의/성서강해> 와 <선택강의> 를 선택하세요 </p>
	</div>
	<div class="content" align="center">
		<a href="/my" style="font-size: 16px;color:white">내가 신청한 강의 보러 가기</a>
	</div>
	<div align="right">
		<form action="/logout">
			<input type="submit" id="submit_button" class="button" value="log out">
		</form>
	</div>
	<hr class="line"/>
	<div class="mainbox">
		<h4 style="color:white"> 1. 주제강의/성서강해 </h4>
		<% for (x in biblelist) { %>
		<div class="item" id="col">
			<a href="/details?t=b&bid=<%= biblelist[x].bid %>" id="b_title" style="color: white"><%= biblelist[x].title %></a>
			<p>강사: <%= biblelist[x].speaker %></p>
			<p>장소: <%= biblelist[x].location %></p>
			<p>수강인원: <%= biblelist[x].current %>/<%= biblelist[x].max %></p>
		</div>
		<% } %>
		<% if (biblelist.length == 0) { %>
		<p style="color:#000; font-size: 30px"> 강의가 없습니다 </p>
		<% } %>
	</div>
	<hr class="line"/>
	<div class="mainbox">
		<h4 style="color:white"> 2. 선택강의 </h4>
		<% for (x in classlist) { %>
		<div class="item" id="col">
			<a href="/details?t=c&cid=<%= classlist[x].cid %>" id="c_title" style="color: white"><%= classlist[x].title %></a>
			<p>강사: <%= classlist[x].speaker %></p>
			<p>장소: <%= classlist[x].location %></p>
			<p>수강인원: <%= classlist[x].current %>/<%= classlist[x].max %></p>
		</div>
		<% } %>
		<% if (classlist.length == 0) { %>
		<p style="color:#000; font-size: 30px"> 강의가 없습니다 </p>
		<% } %>
	</div>
</body>
</html>